# TODO: Implement :)

""" SafetyDaemons job:
        Spawn a thread that monitors behavior of system
        Stops execution if any dangerous conditions are seen
        Conditions could include, but are not limited to:
            position out of safe range [needs definition]
            velocity out of safe range [needs definition]
            current draw too high      [needs definition]
            camera over heating???
            current draw from pump too high?
"""
